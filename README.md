
## Тестовое задание


* Установка:

  * `$ make install`               // установка зависимостей
  * `$ make sqlite-prepare`        // подготовка бд
  * `$ make start`                 // запуск php-сервера